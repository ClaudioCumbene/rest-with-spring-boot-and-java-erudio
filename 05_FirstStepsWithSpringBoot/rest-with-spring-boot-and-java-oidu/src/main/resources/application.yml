cors:
   originPatterns: http://localhost:8080,http://oidu.com.mz,http://localhost:3000
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_spring?useTimezone=true&serverTimezone=UTC
    username: root
    password: oidu
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    # properties:
      # hibernate:
        # dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
